{% extends 'admin/base.html.twig' %}
{% block content %}
    <div class="form-group">
        {% for message in app.flashes('warning') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        <p>
            <b>Module ID: {{ module.id }} </b>
        </p>

        <p>
            <b>Name: {{ module.name }}</b>
        </p>

        <p>
            <b>Measurement unit: {{ module.unit.name }} ( {{ module.unit.designation }} )</b>
        </p>
    </div>

    <div style="width: 40%">
        {% form_theme form 'bootstrap_5_layout.html.twig' %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
